# playwright-project
Repositório de testes utilizando Playwright + Typescript

primeiro passo: refatorar o código e ver se é preciso utilizar a classe de UTILS
segundo passo: antes de subir o código, alterar o install playwright browsers para o npm init latest (documentação do playwright)
após subir o código, validar o ci/cd e em caso de falha, fazer tracking do erro
terceiro passo: após subir o código, escolher um teste (test()) e mudar para test.only
e rodar localmente, e ver se só ele foi executado